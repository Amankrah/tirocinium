import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Guide 5: images go through next/image exclusively. Remote patterns for the
  // signed figure and thumbnail URLs land with the fig:// resolver in Phase 2.3.
};

export default nextConfig;
