# (professor) route group

Professor surfaces (frontend guide 7): sign-in, course settings and seats,
authoring editor, import confirmation, review queue. First routes land in
Phase 2.2 and 2.4. Feature components co-locate here; shared primitives live
in `components/ui`; no component reaches into another feature's directory.
