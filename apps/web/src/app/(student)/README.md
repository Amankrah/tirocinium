# (student) route group

Student surfaces (frontend guide 7): seat-code entry, course home, problem
view, upload flow, the defense conversation. First routes land in Phase 2.2
and 2.3. Feature components co-locate here; shared primitives live in
`components/ui`; no component reaches into another feature's directory.
