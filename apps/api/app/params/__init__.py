"""Parameterization (Phase 5): the parameter spec, the figure-frozen check,
and, in later milestones, auto-parameterization and the generation loop."""

from app.params.routes import router

__all__ = ["router"]
